
import React from 'react';
import ViewStockDashboard from './ViewStockDashboard';

const ViewCurrentStock = ({ isKazo }) => {
  return <ViewStockDashboard isKazo={isKazo} />;
};

export default ViewCurrentStock;
