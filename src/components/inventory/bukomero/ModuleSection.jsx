
import React from "react";

// Given a component to render, renders it (used for the current tab content)
const ModuleSection = ({ children }) => (
  <div>
    {children}
  </div>
);

export default ModuleSection;
