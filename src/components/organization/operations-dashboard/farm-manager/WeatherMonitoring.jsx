
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const WeatherMonitoring = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Weather Monitoring</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Weather tracking, forecasting, and agricultural planning tools will be implemented here.</p>
      </CardContent>
    </Card>
  );
};

export default WeatherMonitoring;
